{
  "name": "block-puzzle-cf-worker",
  "version": "1.0.0",
  "description": "SEANLAB.net Block Puzzle Master on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.43.0"
  }
}
